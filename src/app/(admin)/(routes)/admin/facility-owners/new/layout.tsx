import type { Metadata } from 'next';

export const metadata: Metadata = { title: 'Onboard a facility owner' };

function Layout({ children }: { children: React.ReactNode }) {
	return children;
}

export default Layout;
