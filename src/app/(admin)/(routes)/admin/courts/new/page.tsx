"use client";

import AdminAddCourtView from "../../../../components/views/AdminAddCourtView";

/** Reached from the inventory, so the owner is picked in step one. */
function Page() {
  return <AdminAddCourtView />;
}

export default Page;
